/**
 * Shared constants.
 * Single source of truth for magic strings and configuration values.
 */

// Default group slug for single-group MVP
export const DEFAULT_GROUP_SLUG = 'powerbook';

// Local storage keys
export const STORAGE_KEY_TOKEN = 'powerbook_access_token';
export const STORAGE_KEY_LOCALE = 'powerbook_locale';
